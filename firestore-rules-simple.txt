// Regras simples para desenvolvimento (NÃO usar em produção)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acesso a todos os documentos (apenas para desenvolvimento)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

// ⚠️ ATENÇÃO: Estas regras permitem acesso total aos dados
// Use apenas para desenvolvimento e teste
// Para produção, configure autenticação e regras mais restritivas

